import { PageHero } from "../components/UI";

export function CaseStudiesPage() {
  return (
    <>
      <PageHero
        eyebrow="Case studies"
        title="No standalone case studies yet."
        copy="Project pages contain the current verified evidence. I’ll add case studies only when the supporting artifacts are ready."
      />
    </>
  );
}
