import { PageHero } from "../components/UI";

export function JournalPage() {
  return (
    <>
      <PageHero
        eyebrow="Engineering journal"
        title="No public entries yet."
        copy="I’ll publish original, public-safe engineering notes when the underlying work and evidence are ready."
      />
    </>
  );
}
